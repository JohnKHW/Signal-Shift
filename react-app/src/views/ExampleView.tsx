import SignalShiftView from '@/components/signal-shift/SignalShiftView';

/**
 * url: /
 * @page Signal Shift Home + Scenario Loop
 */
const HomeView = () => {
  return <SignalShiftView />;
};

export default HomeView;
